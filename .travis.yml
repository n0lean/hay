language: python
sudo: true
dist: trusty

python:
  - "3.6"

install:
  - pip install -r test_requirements.txt
  - pip install codecov
  - pip install coverage

script:
  - coverage run -m unittest discover

after_success:
  - codecov

deploy:
  chalice deploy
